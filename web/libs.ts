import equality from "../defs/equality.txt";
import peano from "../defs/peano.txt";
import prop from "../defs/prop.txt";

export const libs: Record<string, string | undefined> = {
	"equality.txt": equality,
	"peano.txt": peano,
	"prop.txt": prop,
};
